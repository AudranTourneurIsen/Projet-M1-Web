import { PlainBookModel } from '@/models';
import React from "react";

export type UserBookFilterLineProps = {
  book: PlainBookModel;
};

export function UserBookFilterLine(props: UserBookFilterLineProps): React.JSX.Element {
  return <span>TODO</span>;
}
